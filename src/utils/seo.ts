const keywords = "generator, random picker, ";
const title = "Keypad - Random picker";
const description = "Keypad is project for focussing faster deployment";

const Seo = {
    keywords,
    title,
    description,
};

export default Seo;
